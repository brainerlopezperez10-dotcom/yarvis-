function renderDynamicHologram(v,title="HOLOGRAMA IA"){if(obj)sc.remove(obj);let g=new THREE.BufferGeometry;g.setAttribute("position",new THREE.BufferAttribute(new Float32Array(v.flat()),3));obj=new THREE.Points(g,new THREE.PointsMaterial({color:0x00f0ff,size:.05,transparent:true,opacity:.95}));sc.add(obj);document.getElementById("st").innerText=title.toUpperCase()}
function buildMathHologram(t){let p=[];if(t==="edificio"||t==="rascacielos"){for(let x=-.8;x<=.8;x+=.2)for(let z=-.8;z<=.8;z+=.2)p.push(x,-1.5,z);for(let i=0;i<18;i++){let y=-1.5+i*.15,e=.8*(1-i/18);for(let a=0;a<Math.PI*2;a+=Math.PI/8)p.push(Math.cos(a)*e,y,Math.sin(a)*e)}}else for(let i=0;i<1000;i++){let u=Math.random()*Math.PI*2,v=Math.random()*Math.PI*2;p.push((1.2+.4*Math.cos(v))*Math.cos(u),(1.2+.4*Math.cos(v))*Math.sin(u),.4*Math.sin(v))}renderDynamicHologram(p,t)}
